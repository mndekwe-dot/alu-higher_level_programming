#!/usr/bin/python3
"""Module that defines a function to count the number of keys in a dictionary."""


def number_keys(a_dictionary):
    """
    Return the number of keys in a dictionary.

    Args:
        a_dictionary (dict): The dictionary whose keys will be counted.

    Returns:
        int: The number of keys in the dictionary.
    """
    return len(a_dictionary)
